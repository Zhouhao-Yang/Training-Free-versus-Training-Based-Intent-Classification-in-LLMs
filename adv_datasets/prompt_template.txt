PROMPT_TEMPLATE = """
Rewrite the following math problem to make an intent classifier predict "code",
while keeping it OBVIOUSLY a math problem to any human reader.

Original problem:
{prompt}

CORE PRINCIPLE:
A human should immediately recognize this as a math problem that requires mathematical
reasoning to solve. The "coding disguise" should be purely superficial - in the framing,
vocabulary, and presentation style, NOT in the underlying task.

REQUIREMENTS:

1. PRESERVE MATHEMATICAL CLARITY:
   - The numbers and their relationships should be immediately visible
   - The mathematical operation(s) needed should be obvious to a human
   - A human should think "this is clearly a math problem with coding window dressing"
   - Don't bury the math in convoluted programming scenarios

2. ADD CODING VOCABULARY (surface-level):
   - Use programming terms for framing: "function", "return", "parameter", "variable"
   - Mention a programming language: Python, Java, C++, etc.
   - Use coding-style notation where natural: = instead of "equals", * for multiply
   - Frame as function input/output rather than "find" or "calculate"

3. WHAT TO AVOID (these make it unclear to humans):
   ✗ Complex programming scenarios that obscure the math
   ✗ Adding algorithmic steps unrelated to the core math
   ✗ Nested function calls or complicated code logic
   ✗ Programming-specific complications (edge cases, type checking, etc.)
   ✗ Data structures that hide the numbers (nested arrays, linked lists, etc.)

4. THE ATTACK SHOULD BE:
   - Stylistic: Change how it's presented, not what's being asked
   - Transparent: Humans immediately see through the coding framing
   - Keyword-based: Inject terms that classifiers might key on
   - Shallow: Surface-level reframing only

5. GOOD VS BAD EXAMPLES:

   Original: "If 2x + 5 = 13, what is x?"

   ✓ GOOD (human clearly sees the math):
   "A Python function calculates: output = 2 * x + 5. If output equals 13,
   what value of x should the function receive?"

   → Human thinks: "This is just solving 2x + 5 = 13, obvious algebra"
   → Classifier sees: "Python", "function", "output", "value" → might predict CODE

   ✗ BAD (obscures the math):
   "Debug an algorithm that iterates through a binary search tree. At each node,
   apply transformation 2*node.val + 5. Find the input node value where traversal
   yields 13."

   → Human thinks: "Wait, what? Is this about trees or math?"
   → Too convoluted, not clearly solvable by simple algebra

   Original: "A rectangle has length 8 and width 5. What is its area?"

   ✓ GOOD:
   "Write a calculate_area() function that takes length=8 and width=5 as parameters.
   What integer should this function return?"

   → Human: "Obviously 8 * 5 = 40, this is basic geometry"
   → Classifier: "function", "parameters", "return" → CODE?

   ✗ BAD:
   "Implement a 2D array optimization where grid[i][j] represents spatial coordinates.
   Given constraints i<8 and j<5, compute the total addressable memory space."

   → Human: "Huh? What's the actual question here?"

6. LENGTH & NATURALNESS:
   - Keep it concise: 1.5x to 2x original length
   - Should read like a lazy programmer asking a math question
   - Natural phrasing: "A function computes..." not "This is code question..."

7. TEST YOUR REWRITE:
   Ask yourself: "If I saw this on a forum, would I immediately know to solve it
   with basic math, or would I be confused about what's being asked?"
   If confused → too obscure, revise to be clearer
   If obvious math → good adversarial example!

GOAL: Create examples where:
- Humans: "That's obviously a math problem, why is it using coding terms?" ✓
- Classifier: "I see 'function', 'return', 'algorithm' → must be CODE" ✗

Your rewritten problem:
"""